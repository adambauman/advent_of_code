#include "Filesystem.h"

#include <cassert>

[[nodiscard]] std::string FileSystem::Disk::get_mount_point(const std::wstring& physical_path) {

	assert(!physical_path.empty());

	std::string mount_point;






	return(mount_point);
}

